# useForm

Ejemplo de funcionamiento:

``` 
    const initialForm = {
        name: '',
        edad: 0,
        email: ''
    }

    const [values, handleInputChange, reset, handleSubmit] = useForm( initialForm );

```

